<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Actualización del Super Héroes/title></title>
    <link rel="stylesheet" href="/Public/style.css">
</head>
<body>
    <h1>Editar Superheroe</h1>
    <form action="/api/superheroes/<%= superheroe._id %>" method="POST">
        <label for="nombreSuperheroe">Nombre del Superhéroe:</label>
        <input type="text" id="nombreSuperheroe" name="nombreSuperheroe" value="<%= superheroe.nombreSuperheroe %>" required>

        <label for="nombreReal">Nombre Real:</label>
        <input type="text" id="nombreReal" name="nombreReal" value="<%= superheroe.nombreReal %>" required>

        <label for="edad">Edad:</label>
        <input type="number" id="edad" name="edad" value="<%= superheroe.edad %>" required>

        <label for="planetaOrigen">Planeta Origen</label>
        <input type="text" id="planetaOrigen" name="planetaOrigen" value="<%= superheroe.planetaOrigen %>" required>

        <label for="debilidad">Debilidad</label>
        <input type="text" id="debilidad" name="debilidad" value="<%= superheroe.debilidad %>" required>

        <label for="poder">Poder</label>
        <textarea id="poder" name="poder[]" required><%= superheroe.poder.join(', ') %></textarea>

        <label for="aliado">Aliado</label>
        <input type="text" id="aliado" name="aliado" value="<%= superheroe.aliado %>" required>

        <label for="enemigo">Enemigo</label>
        <input type="text" id="enemigo" name="enemigo" value="<%= superheroe.enemigo %>" required>

        <label for="creador">Creador</label>
        <textarea id="creador" name="creador[]" required><%= superheroe.creador.join(', ') %></textarea>

        <button type="submit">Actualizar Superhéroe</button>
    </form>
    <a href="/api/superheroes">Volver al Dashboard</a>
    <% if (errors.length > 0 ) { %>
        <ul>
            <% errors.forEach(function(error) { %>
                <li><%= error.mgs %></li> <!--Cambiado de error.mgs a error.msg-->
            <% }) %>
        </ul>
    <% } %>
</body>
</html>